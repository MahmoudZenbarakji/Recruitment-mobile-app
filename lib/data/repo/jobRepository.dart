/*import 'package:your_app/data/models/job_model.dart';
import 'package:your_app/data/api/job_api_service.dart';

class JobRepository {
  final JobApiService api;

  JobRepository(this.api);

  Future<List<JobModel>> getJobs({required int page, required int limit}) async {
    final response = await api.fetchJobs(page: page, limit: limit);
    return response;
  }
}
*/